# Examples

Example QGIS projects, sample IFC files, and sample layers will live here.

Large IFC files should not be committed without an explicit decision.

IFC sample files must be raw STEP/IFC files starting with `ISO-10303-21;`.
When downloading from GitHub, use the raw file URL instead of saving the HTML
repository page with an `.ifc` extension.
